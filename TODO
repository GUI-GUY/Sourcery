beta 0.1
+ big Selector checkbuttons
+ big Selector back
+ stop
+ mousewheel scrolling
+ scrolling accuracy
+ write tutorial
+ light theme
+ array memory leaks
+ big selector memory leaks
+ big selector: image data data image
+ what happens when search finished (Currently Sourcing)
+ frame2 grid_forget
+ block sourcery button while processing
+ reset scrollbars
+ Big Selector in save
+ (untested) sourcery error stop
~ comments
+ create theme and credentials
+ beautify
+ Fehlerbehandlung
+ rework save system
Last Issue: 0023


beta 0.2
+ Options is now class
+ Files is now class
+ added globalvariables module
+ logfile
log on screen
+ (u) rename options
+ (u) minsim options
+ better pixiv login
more metadata(tags, source, illustrator, uncensored)
better big Selector
previews widgets for custom theme
theme errors(wrong colour etc.)
next & prev in big Selector
minileak in results
better sync for stop
scrolling accuracy?
in window tutorial
no message boxes but labels
Fehlerbehandlung
checking a folder keeps it 100%
Last Issue: 0029

beta 0.3
2nd service
service options
png over jpg over high res options
optimizations
foreign folder support

All arrays: (+: no memory leaks 10.01.2020)
+ input_images_array = [] # For all images in Input folder
+- pixiv_images_array = [] # For all images in sourced_progress/pixiv folder
+- delete_dirs_array = [] # For empty directories or dirs where no original is present 
+- chkbtn_vars_big_array = [] # [[imgname, (img, IntVar), (img, IntVar) ...]...]
+? safe_to_show_array
+ chkbtn_vars_array = [] # 12x2 Checkbutton variables for the results screen

+ credentials_array = read_credentials(cwd) # [0]SauceNao API-Key, [1]Pixiv Username, [2]Pixiv Password, [3]Pixiv refreshtoken

+ colour_array = [] # For all current theme colours
+ custom_array = [] # Custom theme colours

comm_q = Queue() # Queue for 'Remaining searches'
comm_img_q = Queue() # Queue for 'Currently Sourcing'

Logfile contains:
+ Time
+ All Errors
+ Successful options change